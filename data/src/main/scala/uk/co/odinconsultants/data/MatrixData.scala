package uk.co.odinconsultants.data

object MatrixData {

  type Range[T] = (T, T)

//  type RandFn[T] = RandFn[T] => (T, RandFn[T])

  def randomCoords[T](n: Int, ranges: Seq[Range[T]]): Seq[Seq[T]] = {
    ???
  }

}
